import 'package:flutter/material.dart';

class PLusView extends StatelessWidget {
  const PLusView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Plus View"));
  }
}
