import 'package:flutter/material.dart';

class GroubView extends StatelessWidget {
  const GroubView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("GroubView"));
  }
}
